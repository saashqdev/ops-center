# Ops-Center E2E Test Configuration
# Copy this file to .env.test and update with your values

# Base URL for Ops-Center
# Change this if testing a different environment
BASE_URL=https://your-domain.com

# Test User Credentials (Keycloak SSO)
# Create a dedicated test user with appropriate permissions
TEST_USERNAME=aaron
TEST_PASSWORD=your-admin-password

# Optional: Playwright Configuration
# Number of parallel workers (default: 1 for E2E tests)
PLAYWRIGHT_WORKERS=1

# Timeout for tests in milliseconds (default: 60000 = 1 minute)
PLAYWRIGHT_TIMEOUT=60000

# CI mode (set to true when running in CI/CD)
# CI=true

# Optional: Enable/disable specific features
# ENABLE_VIDEO_RECORDING=true
# ENABLE_TRACING=true
# HEADLESS_MODE=true
