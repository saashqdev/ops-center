# Database Backup Configuration
# Add these to your .env file or docker-compose.yml

# Backup directory (will be created if doesn't exist)
BACKUP_DIR=/app/backups/database

# Number of days to keep backups
BACKUP_RETENTION_DAYS=7

# Maximum number of backups to keep (regardless of age)
BACKUP_MAX_COUNT=30

# Interval between automatic backups (in hours)
# Default: 24 (daily backups)
BACKUP_INTERVAL_HOURS=24

# Enable/disable automatic backups on startup
# Set to 'false' to disable scheduled backups
BACKUP_ENABLED=true
