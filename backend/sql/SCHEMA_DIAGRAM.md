# Extensions Marketplace Schema Diagram

## Entity Relationship Diagram (ERD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         EXTENSIONS MARKETPLACE SCHEMA                       │
│                                8 Core Tables                                 │
└─────────────────────────────────────────────────────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                              PRODUCT CATALOG                                │
└────────────────────────────────────────────────────────────────────────────┘

                            ┌─────────────────────┐
                            │      add_ons        │
                            │─────────────────────│
                            │ PK id (UUID)        │
                            │    name             │
                            │    description      │
                            │    category         │
                            │    base_price       │
                            │    billing_type     │
                            │    features (JSONB) │
                            │    is_active        │
                            │    metadata (JSONB) │
                            │    created_at       │
                            │    updated_at       │
                            └──────────┬──────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
                    │                  │                  │
        ┌───────────▼──────────┐  ┌───▼─────────────┐  ┌▼──────────────────┐
        │   user_add_ons       │  │  cart_items     │  │ add_on_features   │
        │──────────────────────│  │─────────────────│  │───────────────────│
        │ PK id (UUID)         │  │ PK id (UUID)    │  │ PK id (UUID)      │
        │    user_id           │  │    user_id      │  │ FK add_on_id      │
        │ FK add_on_id ────────┤  │ FK add_on_id    │  │    feature_key    │
        │    status            │  │    quantity     │  │    enabled        │
        │    purchased_at      │  │    added_at     │  │    configuration  │
        │    expires_at        │  │    updated_at   │  │    (JSONB)        │
        │    lago_sub_id       │  └─────────────────┘  │    created_at     │
        │    auto_renew        │                        │    updated_at     │
        │    created_at        │                        └───────────────────┘
        │    updated_at        │
        └──────────────────────┘                        UNIQUE(add_on_id,
        UNIQUE(user_id,                                       feature_key)
               add_on_id)


┌────────────────────────────────────────────────────────────────────────────┐
│                          TRANSACTION HISTORY                                │
└────────────────────────────────────────────────────────────────────────────┘

                            ┌─────────────────────┐
                            │      add_ons        │
                            │      (above)        │
                            └──────────┬──────────┘
                                       │
                                       │
                            ┌──────────▼──────────────────┐
                            │   add_on_purchases          │
                            │─────────────────────────────│
                            │ PK id (UUID)                │
                            │    user_id                  │
                            │ FK add_on_id ───────────────┤
                            │    amount                   │
                            │    currency                 │
                            │    stripe_payment_id        │
                            │    lago_invoice_id          │
                            │    status                   │
                            │    payment_method           │
                            │    refund_amount            │
                            │    refunded_at              │
                            │    metadata (JSONB)         │
                            │    purchased_at             │
                            │    created_at               │
                            └─────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                        PRICING & PROMOTIONS                                 │
└────────────────────────────────────────────────────────────────────────────┘

        ┌─────────────────────┐
        │      add_ons        │
        │      (above)        │
        └──────────┬──────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    │              │              │
┌───▼──────────────┐  ┌──────────▼────────────┐
│ pricing_rules    │  │  add_on_bundles       │
│──────────────────│  │───────────────────────│
│ PK id (UUID)     │  │ PK id (UUID)          │
│ FK add_on_id ────┤  │    name               │
│    tier_code     │  │    description        │
│    discount_%    │  │    add_on_ids (UUID[])│ ◄─┐ References
│    discount_amt  │  │    discount_percent   │   │ multiple
│    conditions    │  │    discount_amount    │   │ add_ons
│    (JSONB)       │  │    is_active          │   │ via array
│    priority      │  │    valid_from         │   │
│    is_active     │  │    valid_until        │   │
│    valid_from    │  │    max_uses           │   │
│    valid_until   │  │    current_uses       │   │
│    created_at    │  │    metadata (JSONB)   │   │
│    updated_at    │  │    created_at         │   │
└──────────────────┘  │    updated_at         │   │
                      └───────────────────────┘   │
                                                   │
                      ┌───────────────────────┐   │
                      │ promotional_codes     │   │
                      │───────────────────────│   │
                      │ PK id (UUID)          │   │
                      │    code (UNIQUE)      │   │
                      │    discount_type      │   │
                      │    discount_value     │   │
                      │    max_uses           │   │
                      │    current_uses       │   │
                      │    applicable_to ─────┼───┘
                      │    (UUID[])           │
                      │    min_purchase_amt   │
                      │    user_restrictions  │
                      │    (JSONB)            │
                      │    expires_at         │
                      │    is_active          │
                      │    created_by         │
                      │    created_at         │
                      │    updated_at         │
                      └───────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                        EXTERNAL INTEGRATIONS                                │
└────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│   KEYCLOAK      │         │     STRIPE       │         │      LAGO        │
│   (Auth)        │         │   (Payments)     │         │   (Billing)      │
├─────────────────┤         ├──────────────────┤         ├──────────────────┤
│ user_id         │────────►│ stripe_payment_id│────────►│ lago_subscription│
│ (VARCHAR 255)   │         │ (VARCHAR 255)    │         │ lago_invoice_id  │
└─────────────────┘         └──────────────────┘         └──────────────────┘
        │                            │                            │
        │                            │                            │
        └────────────┬───────────────┴────────────┬───────────────┘
                     │                            │
                     │                            │
            ┌────────▼───────────────────────────▼────────┐
            │       Extensions Marketplace Tables         │
            │  - user_add_ons (user_id, lago_sub_id)     │
            │  - add_on_purchases (user_id, stripe_id,   │
            │                      lago_invoice_id)       │
            │  - cart_items (user_id)                     │
            └─────────────────────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                          REFERENCE TABLES                                   │
│                        (Existing in unicorn_db)                            │
└────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐         ┌─────────────────────┐
│ subscription_tiers  │         │ feature_definitions │
├─────────────────────┤         ├─────────────────────┤
│ tier_code (PK)      │◄────────│ feature_key (PK)    │
│ tier_name           │    │    │ feature_name        │
│ monthly_price       │    │    │ description         │
│ features            │    │    │ category            │
└─────────────────────┘    │    └─────────────────────┘
         ▲                 │             ▲
         │                 │             │
         │                 │             │
    ┌────┴──────┐          │      ┌──────┴────────┐
    │ pricing_  │          └──────│ add_on_       │
    │ rules     │                 │ features      │
    │ (tier_    │                 │ (feature_key) │
    │  code)    │                 └───────────────┘
    └───────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                        KEY RELATIONSHIPS SUMMARY                            │
└────────────────────────────────────────────────────────────────────────────┘

1. add_ons → user_add_ons (1:N)
   - One add-on can have many user subscriptions
   - CASCADE delete (remove subscriptions if add-on deleted)

2. add_ons → add_on_purchases (1:N)
   - One add-on can have many purchase transactions
   - RESTRICT delete (prevent deletion if purchases exist)

3. add_ons → cart_items (1:N)
   - One add-on can be in many carts
   - CASCADE delete (remove from carts if add-on deleted)

4. add_ons → add_on_features (1:N)
   - One add-on can enable many features
   - CASCADE delete (remove features if add-on deleted)

5. add_ons → pricing_rules (1:N)
   - One add-on can have many pricing rules
   - CASCADE delete (remove rules if add-on deleted)

6. add_ons → add_on_bundles (N:M via array)
   - Bundles contain multiple add-ons (stored as UUID array)
   - Flexible relationship without junction table

7. add_ons → promotional_codes (N:M via array)
   - Promo codes can apply to specific add-ons (UUID array)
   - NULL array means applies to all add-ons


┌────────────────────────────────────────────────────────────────────────────┐
│                          INDEX STRATEGY                                     │
└────────────────────────────────────────────────────────────────────────────┘

🔍 B-TREE INDEXES (Standard lookup)
   ├── user_add_ons(user_id) ────────────► Most critical - high query volume
   ├── cart_items(user_id)
   ├── add_on_purchases(user_id, purchased_at DESC)
   ├── add_ons(category)
   └── promotional_codes(code) ──────────► Unique index

🔍 GIN INDEXES (JSONB and arrays)
   ├── add_ons(features) ────────────────► JSONB feature queries
   ├── add_on_features(configuration) ───► JSONB config queries
   ├── add_on_bundles(add_on_ids) ───────► Array containment queries
   └── promotional_codes(applicable_to) ─► Array containment queries

🔍 COMPOSITE INDEXES (Multi-column queries)
   ├── user_add_ons(user_id, add_on_id) ► Check ownership
   ├── user_add_ons(user_id, status, add_on_id) ► Active subscriptions
   └── add_on_purchases(user_id, purchased_at DESC) ► Purchase history

🔍 PARTIAL INDEXES (Filtered subsets)
   ├── add_ons WHERE is_active = TRUE
   ├── user_add_ons WHERE status = 'active'
   └── promotional_codes WHERE is_active = TRUE AND expires_at > NOW()


┌────────────────────────────────────────────────────────────────────────────┐
│                        DATA FLOW DIAGRAM                                    │
└────────────────────────────────────────────────────────────────────────────┘

   USER JOURNEY: BROWSING → CART → CHECKOUT → ACTIVE SUBSCRIPTION

   1. BROWSE MARKETPLACE
      ┌─────────┐
      │  User   │
      └────┬────┘
           │ Query: Get active add-ons
           ▼
      ┌──────────────┐
      │   add_ons    │ SELECT * WHERE is_active = TRUE
      └──────────────┘

   2. ADD TO CART
      ┌─────────┐
      │  User   │
      └────┬────┘
           │ Action: Add to cart
           ▼
      ┌──────────────┐
      │ cart_items   │ INSERT (user_id, add_on_id, quantity)
      └──────────────┘

   3. CHECKOUT PROCESS
      ┌─────────┐
      │  User   │
      └────┬────┘
           │ Action: Checkout
           ├──────────────────────────┐
           │                          │
           ▼                          ▼
      ┌──────────────┐          ┌──────────────┐
      │ promotional_ │          │ pricing_rules│
      │    codes     │◄─────────│              │
      └──────────────┘          └──────────────┘
           │ Apply discounts         │
           └──────────┬───────────────┘
                      │
                      ▼
              ┌──────────────┐
              │   STRIPE API │ Payment processing
              └──────┬───────┘
                     │
                     ▼
      ┌───────────────────────────────┐
      │    add_on_purchases           │ INSERT transaction record
      │  (stripe_payment_id, amount)  │
      └──────────────┬────────────────┘
                     │
                     ▼
      ┌───────────────────────────────┐
      │      LAGO API                 │ Create subscription
      └──────────────┬────────────────┘
                     │
                     ▼
      ┌───────────────────────────────┐
      │    user_add_ons               │ INSERT active subscription
      │  (lago_subscription_id)       │
      └──────────────┬────────────────┘
                     │
                     ▼
      ┌───────────────────────────────┐
      │    cart_items                 │ DELETE purchased items
      └───────────────────────────────┘

   4. FEATURE ACCESS
      ┌─────────┐
      │  User   │
      └────┬────┘
           │ Request: Access feature
           ▼
      ┌──────────────┐
      │ user_add_ons │ Check: WHERE user_id = ? AND status = 'active'
      └──────┬───────┘
             │
             ├──────────────────┐
             │                  │
             ▼                  ▼
      ┌──────────────┐    ┌──────────────┐
      │   add_ons    │    │add_on_features│
      └──────┬───────┘    └──────┬────────┘
             │                   │
             └──────────┬────────┘
                        │
                        ▼
                   ┌─────────┐
                   │ GRANTED │
                   └─────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                        TABLE SIZE ESTIMATES                                 │
│                         (After 1 Year - 10K Users)                         │
└────────────────────────────────────────────────────────────────────────────┘

│ Table                  │ Rows (Est.) │ Size (Est.) │ Growth Rate │
├────────────────────────┼─────────────┼─────────────┼─────────────┤
│ add_ons                │         50  │     50 KB   │ Low         │
│ add_on_features        │        200  │    100 KB   │ Low         │
│ promotional_codes      │        100  │     50 KB   │ Low         │
│ add_on_bundles         │         20  │     20 KB   │ Low         │
│ pricing_rules          │        150  │     75 KB   │ Low         │
├────────────────────────┼─────────────┼─────────────┼─────────────┤
│ user_add_ons           │     30,000  │     15 MB   │ Medium      │
│ cart_items             │      5,000  │      2 MB   │ Medium      │
│ add_on_purchases       │    100,000  │     50 MB   │ High        │
├────────────────────────┼─────────────┼─────────────┼─────────────┤
│ TOTAL (without indexes)│    135,520  │     67 MB   │             │
│ TOTAL (with indexes)   │            │    ~120 MB   │             │
└────────────────────────┴─────────────┴─────────────┴─────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                           LEGEND                                            │
└────────────────────────────────────────────────────────────────────────────┘

PK  = Primary Key
FK  = Foreign Key
──► = One-to-Many relationship
◄── = Referenced by
[N] = Array/Multiple values
{ } = JSONB object

